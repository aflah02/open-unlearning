# @package _global_
defaults:
  - override /eval: custom_mia
  - _self_

mia_corpus: ???
mia_data_root: ${paths.work_dir}/../prepared_data
task_name: custom_mia_${mia_corpus}
paths:
  output_dir: saves/eval/${task_name}

eval:
  custom_mia:
    output_dir: ${paths.output_dir}
