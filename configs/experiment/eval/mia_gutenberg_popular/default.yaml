# @package _global_
defaults:
  - override /eval: mia_gutenberg_popular
  - _self_

task_name: mia_gutenberg_popular
paths:
  output_dir: saves/eval/${task_name}

eval:
  mia_gutenberg_popular:
    output_dir: ${paths.output_dir}