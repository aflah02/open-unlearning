# @package eval.custom_mia
defaults:
  - custom_mia_metrics:
    - mia_loss
    - mia_min_k
    - mia_min_k_plus_plus
    - mia_gradnorm
    - mia_zlib

handler: TOFUEvaluator
output_dir: ${paths.output_dir}
metrics: {}
overwrite: false
batch_size: 32